Command to manage the environment variables (session-only or global).

## Syntax

List all environment variables:

```shell
$Env
```

Update or set a variable
```shell
$Env:JAVA_HOME="C:\Program Files\Java\jdk17"
```

